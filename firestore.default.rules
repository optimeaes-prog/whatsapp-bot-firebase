rules_version = '2';
// Rules for the (default) database - empty, no data here
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
